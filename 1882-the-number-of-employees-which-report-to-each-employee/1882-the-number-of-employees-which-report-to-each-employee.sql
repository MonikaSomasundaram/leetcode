# Write your MySQL query statement below
select a.employee_id,a.name,count(*) as reports_count,round(avg(b.age)) as average_age from Employees as a,Employees as b where a.employee_id=b.reports_to group by employee_id order by employee_id;